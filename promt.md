
**Промт для дальнейшей разработки игры "Survival Game"**  

**Текущее состояние проекта:**  
- Реализовано базовое API на FastAPI (Python 3.9+) с роутами:  
  - `GET /players/{id}` — данные игрока  
  - `GET /inventory/{id}` — инвентарь  
  - `POST /items/` — создание предмета  
- Настроен фронтенд (React) с компонентами:  
  - `Player` — отображение характеристик  
  - `Inventory` — список предметов  
- Используется Pydantic для валидации данных.  

**Следующие шаги для разработки:**  

### 1. **Расширение функционала инвентаря**  
**Backend (FastAPI):**  
- Добавить методы:  
  - `POST /inventory/{player_id}/add` — добавить предмет  
  - `DELETE /inventory/{player_id}/{item_id}` — удалить предмет  
  - `PUT /inventory/{player_id}/{item_id}` — обновить количество  
- Реализовать проверку:  
  - Максимальный размер инвентаря  
  - Уникальность предметов  

**Frontend (React):**  
- Добавить UI-элементы:  
  - Кнопки "Добавить/Удалить предмет"  
  - Модальное окно для выбора предмета  
  - Валидация форм  

---

### 2. **Подключение базы данных**  
**Выбор БД:** SQLite (для разработки) / PostgreSQL (продакшен)  
**Задачи:**  
- Настроить SQLAlchemy (ORM)  
- Создать модели:  
  - `Player` (id, name, health)  
  - `Item` (id, name, type)  
  - `Inventory` (player_id, item_id, quantity)  
- Реализовать миграции (Alembic)  

---

### 3. **Система крафта**  
**Требования:**  
- Роуты:  
  - `GET /recipes/` — список рецептов  
  - `POST /craft/` — создание предмета  
- Модели:  
  - `Recipe` (id, required_items, result_item)  
- Логика:  
  - Проверка наличия ресурсов  
  - Обновление инвентаря  

---

### 4. **Аутентификация игроков**  
- Реализовать:  
  - Регистрацию (`POST /auth/register`)  
  - Логин (`POST /auth/login`) с JWT  
  - Защищенные роуты (FastAPI `Depends`)  

---

### 5. **Дополнительные системы**  
- **Локации:**  
  - `GET /locations/` — список зон  
  - `POST /explore/` — исследование (ресурсы/враги)  
- **Боевая механика:**  
  - `POST /battle/` — атака/защита  
  - Урон, уровень сложности  

**Выберите приоритетное направление (1-5) или предложите свой вариант.**  

**Технические требования:**  
- Код должен быть модульным и покрыт тестами (pytest).  
- Для фронтенда — использовать React Hooks + Context API.  
- Документировать API (Swagger/OpenAPI).  

**Формат ответа:**  
```markdown
1. Выбранный пункт: [номер]  
2. Конкретные задачи: [список]  
3. Предложения по реализации: [если есть]  
```

### **Промт для разработки игры в стиле "Земля Мертвых: Survival"**  

**🔹 Текущее состояние проекта:**  
- Реализовано базовое API (FastAPI) с роутами для игрока и инвентаря.  
- Настроен фронтенд (React) с отображением данных.  
- Используется Pydantic для валидации.  

**🔹 Основные механики из "Земля Мертвых: Survival" для реализации:**  

### **1. Выживание и характеристики игрока**  
**Backend:**  
- Модель игрока с параметрами:  
  ```python
  class Player(BaseModel):
      health: int = 100       # Здоровье (0 = смерть)
      hunger: int = 100       # Голод (0 → -health)
      thirst: int = 100       # Жажда (0 → -health)
      stamina: int = 100      # Выносливость (бег/атака)
      radiation: int = 0      # Радиация (при достижении 100 → смерть)
  ```  
- Роуты:  
  - `POST /player/eat` (восстановление голода)  
  - `POST /player/drink` (восстановление жажды)  
  - `GET /player/status` (текущие показатели)  

**Frontend:**  
- Индикаторы характеристик (полоски здоровья, голода и т.д.).  
- Эффекты при критических значениях (например, дрожание экрана при голоде).  

---

### **2. Система инвентаря и крафта**  
**Backend:**  
- Предметы с типами: `еда`, `оружие`, `ресурсы`, `аптечки`.  
- Крафт-рецепты (например, `Доски + Гвозди = Ящик`).  
- Роуты:  
  - `POST /craft` (создание предмета)  
  - `GET /recipes` (доступные рецепты)  

**Frontend:**  
- Грид-инвентарь с drag-and-drop.  
- Окно крафта с визуальным отображением рецептов.  

---

### **3. Исследование локаций и добыча ресурсов**  
**Backend:**  
- Локации: `Лес`, `Город`, `Бункер`, `Военная база`.  
- Роуты:  
  - `POST /location/explore` (получить ресурсы/встретить врага)  
  - `GET /location/current` (текущая локация игрока)  

**Frontend:**  
- Карта с выбором зоны.  
- Анимация исследования (таймер + случайный исход).  

---

### **4. Боевая система (PvE)**  
**Backend:**  
- Типы врагов: `зомби`, `бандиты`, `мутанты`.  
- Роуты:  
  - `POST /battle/attack` (атака)  
  - `POST /battle/escape` (побег)  

**Frontend:**  
- Кнопки действий (`Атаковать`, `Бежать`).  
- Лог боя (как в классических RPG).  

---

### **5. Погодные условия и цикл день/ночь**  
**Backend:**  
- Динамическая смена времени суток (каждые 30 мин игрового времени).  
- Погода: `дождь`, `радиоактивная буря`, `ясно`.  
- Эффекты:  
  - Ночь: меньше видимость, больше врагов.  
  - Дождь: +20% к шуму (враги быстрее замечают).  

**Frontend:**  
- Затемнение экрана ночью.  
- Часы с игровым временем.  

---

### **6. Квесты и задания**  
**Backend:**  
- Типы квестов:  
  - `Сбор ресурсов` (принести 10 дерева)  
  - `Уничтожение` (убить 5 зомби)  
- Роуты:  
  - `GET /quests` (активные квесты)  
  - `POST /quests/complete` (завершение)  

**Frontend:**  
- Вкладка "Журнал заданий".  
- Уведомления о прогрессе.  

---

### **7. Мультиплеер (опционально)**  
- Реализовать через WebSockets:  
  - Общий чат.  
  - Торговля между игроками.  
  - PvP-зоны.  

---

### **🔧 Технические требования:**  
- **Backend:**  
  - FastAPI + SQLAlchemy (PostgreSQL).  
  - Redis для кэширования и таймеров.  
- **Frontend:**  
  - React + Phaser (для сложной графики).  
  - Анимации через CSS/GSAP.  
- **Тестирование:**  
  - pytest для бэкенда.  
  - Jest + React Testing Library для фронтенда.  

---

### **📌 Что реализуем в первую очередь?**  
```markdown
1. [ ] Система характеристик (голод/жажда)  
2. [ ] Расширенный инвентарь + крафт  
3. [ ] Исследование локаций  
4. [ ] Боевая система  
5. [ ] Погода и время  
6. [ ] Квесты  
7. [ ] Мультиплеер (если время)  
```  

**Формат ответа:**  
```markdown
Выбранные пункты: [номера]  
Дополнительные пожелания: [текст]  
```